interface:
  display_name: GG → Git Submodules → Config
  short_description: Submodule config, .gitmodules precedence, and worktree overrides
  icon_small: ./assets/icon-small.svg
  icon_large: ./assets/icon-large.png
  brand_color: '#6366F1'
  default_prompt: >-
    Use when applying config layer precedence for submodule branch overrides, deciding between
    .git/config and config.worktree, understanding update --remote vs gitlink SHA semantics, or
    migrating away from tracked .gitmodules drift without unsupported hacks. $git-submodules.
    Reference git-submodules/SKILL.md when cross-referencing this skill.
